function bundle_macports {
  bundle config build.nokogiri --with-xml2-dir=/usr --with-xslt-dir="$MACPORTS_ROOT/local" --with-iconv-dir="$MACPORTS_ROOT/local"
  bundle check > /dev/null || bundle --local --path vendor/bundle
}
