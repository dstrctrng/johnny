#!/bin/bash -e

nm_base=$1; shift
bin/bundler

figlet "build box"
bundle exec vagrant basebox build $nm_base -f -n

figlet "exporting box"
bundle exec vagrant basebox export $nm_base
cp $nm_base.box definitions/$nm_base/
rm -f $nm_base.box

bin/setup-box $nm_base
